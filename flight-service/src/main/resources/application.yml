server:
  port: 8080

spring:
  application:
    name: flight-service
  datasource:
    url: jdbc:mysql://localhost:3306/fms
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


